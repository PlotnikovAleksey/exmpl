os: linux
language: 
- cpp

jobs:
  include:
  - name: "building"
    script:
    - cmake -H. -B_build -DBUILD_TESTS=ON
    - cmake --build _build
  - name: "tests"
    script:
    - cmake -H. -B_build -DBUILD_TESTS=ON
    - cmake --build _build
    - ./_build/tests
